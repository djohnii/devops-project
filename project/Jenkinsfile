pipeline {
    agent {
        label "agent-1"
    } 
    environment {
        registry = "alwx1753/devops-project"
        registryCredential = 'dockerhub'
        dockerImage = ''
    }
    stages {
        stage('check git'){
            steps{
                script{
                    git branch: 'main', url: 'https://github.com/djohnii/devops-project.git'
                }
            }
        }
        stage('build docker image'){
            steps{
                script{
                    dir('project/docker'){
                        dockerImage = docker.build registry + ":$BUILD_NUMBER"
                    }
                }
            }
        }
        stage('deploy image') {
            steps{
                script{
                    docker.withRegistry( '', registryCredential ) {
                        dockerImage.push()
                    }
                }
            }
        }
    }
}
